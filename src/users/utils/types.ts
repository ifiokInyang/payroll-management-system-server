export type CreateUserDetails = {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  company: string;
  salary: string;
};

export type UpdateUserDetails = {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  company: string;
  salary: string;
};
